{
  "displayName": "RetroStore comparator difference (disabled pending channel)",
  "combiner": "OR",
  "enabled": false,
  "conditions": [
    {
      "displayName": "Comparator approval gate failed",
      "conditionMatchedLog": {
        "filter": "resource.type=\"cloud_run_job\"\nresource.labels.job_name=\"retrostore-hourly-comparator\"\njsonPayload.event=\"scheduled_comparison\"\njsonPayload.approval_gate.passes=false"
      }
    }
  ],
  "alertStrategy": {
    "notificationRateLimit": {
      "period": "300s"
    },
    "autoClose": "604800s"
  },
  "documentation": {
    "mimeType": "text/markdown",
    "subject": "RetroStore compatibility comparison failed",
    "content": "Stop any migration soak or canary. Open the `artifact_uri` from the matching structured log, verify its SHA-256, and classify every difference. Production stays on or returns to App Engine."
  },
  "userLabels": {
    "service": "retrostore",
    "gate": "comparison"
  }
}
